{
  "name": "kalaayana-studios",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:staging": "npm run build && bash scripts/stamp.sh staging",
    "preview": "astro preview",
    "check": "python3 scripts/check.py"
  },
  "devDependencies": {
    "astro": "^7.3.1",
    "sharp": "^0.34.2"
  }
}
